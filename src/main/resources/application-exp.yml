server:
  port: 9090
  servlet:
    context-path: /hospital_api

spring:
  application:
    name: hospital
  datasource:
    name: test
    url:  jdbc:mysql://localhost:3306/def
    username: root
    password: root
    driver:
      class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update


